# crawler_basic.py dependencies
beautifulsoup4>=4.12
requests>=2.31
nltk>=3.8
pymongo>=4.6
schedule>=1.2

# app.py (Streamlit GUI) dependency
streamlit>=1.30

# Needed because PurePortal serves a Cloudflare "managed challenge" page to
# plain requests/headless Selenium (robots.txt itself permits crawling).
# undetected-chromedriver patches the automation fingerprints Cloudflare
# checks for. Needs Chrome installed locally, and run headed (not headless) --
# see setup_driver() / wait_for_cloudflare() in crawler_basic.py.
selenium>=4.15
undetected-chromedriver>=3.5

# One-time setup: python -c "import nltk; nltk.download('punkt'); nltk.download('punkt_tab'); nltk.download('stopwords')"
